import React, { useState } from 'react';

const useGoodsList = (goodsList) => {
    
    const isLoading = true;
    // const subtract = () => setCount(count - 1);
    return { isLoading };
}

export default useGoodsList;